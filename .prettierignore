# Ignore files for PNPM, NPM and YARN
pnpm-lock.yaml
package-lock.json
yarn.lock

# Vendored third-party bundle — the standalone pdf.js viewer app used by the
# PDF reader iframe. Not published on npm, so it stays checked in.
static/pdfjs/

# Third-party model + CSL data shipped as Tauri resources. tokenizer.json is read by
# the Rust `tokenizers` crate; reformatting it churns 60k lines for no benefit.
src-tauri/resources/

# Build output and generated artifacts
build/
coverage/
.svelte-kit/
src-tauri/target/
src-tauri/gen/

# Agent worktrees hold full nested checkouts of this repo
.claude/
