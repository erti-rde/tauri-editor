# M7a / M7b — Release pipeline, beta, 1.0

## M7a — Release pipeline (runs alongside M1a)

### M7a-1 Rewrite `release.yaml`

Refs: REL · Depends on: —

- AC-1 Implements the docs/release.md pipeline: verify → reuse ci.yaml → a matrix of 4 targets
  → smoke test → draft release with `SHA256SUMS.txt` and `latest.json`.
- AC-2 Fixes all ten listed defects, one checklist line each in the PR.
- AC-3 A dry run on a `v0.0.0-rc` tag in a fork, or with drafts deleted after, produces all
  artefacts.
- AC-4 The smoke test fails when the model file is removed from resources (demonstrated once).

### M7a-2 Updater keys

Refs: SEC-9

- AC-1 Keypair generated offline **by the maintainer** (Claude doesn't handle private keys).
  The pubkey is committed, the private key and password are repo secrets.
- AC-2 `docs/release.md` gains a custody and rotation section.

### M7a-3 Updater behind consent

Refs: UX-1, UX-10, SEC-8 · Depends on: M7a-2

- AC-1 `tauri-plugin-updater` checks only if the user enabled it (first-run switch or Settings
  › About); **Check now** always works.
- AC-2 The README's list gains "Checking for updates: your version number, to github.com".
- AC-3 A Rust network inventory test lists crates with HTTP features, so a new one fails
  review.

### M7a-4 Dependency audit and pinning

Refs: SEC-7

- AC-1 `pnpm audit --prod` and `cargo audit` run in CI (non-blocking until the 1.0 tag, then
  blocking).
- AC-2 Third-party actions in the release workflow are pinned by commit SHA.

### M7a-5 Windows signing application

- AC-1 An application submitted to SignPath Foundation, and the outcome recorded in plan §6. If
  accepted, the pipeline signs, and the first-open guide drops the SmartScreen step.
- AC-2 The application needs the maintainer's identity, so the maintainer submits it; Claude
  prepares the text.

### M7a-6 macOS self-signed identity

- AC-1 A self-signed code-signing certificate, created by the maintainer, stored as a CI
  secret, used for every macOS build. Ad-hoc is the fallback.
- AC-2 Verified in the beta: two updates in a row keep Documents-folder access.

### M7a-7 Release script and changelog

- AC-1 `pnpm release X.Y.Z` bumps the three version files, writes the `git-cliff` changelog
  section, commits and tags.
- AC-2 The changelog calls out format and schema changes in their own section.

### M7a-8 First-open guide

- AC-1 A per-platform page with screenshots (macOS 15+ Open Anyway, Windows SmartScreen,
  Linux AppImage/deb) and checksum verification.
- AC-2 Linked from the README, the landing page and release notes; written in the README's
  voice, including why.

### M7a-9 Identity and window

Refs: #39

- AC-1 Logo and icon set in every required size; the default window is 1280×800, with a
  minimum of 960×600.

### M7a-10 About

Refs: UX-10

- AC-1 Settings › About: version, AGPL-3.0, source link, generated third-party notices (npm and
  cargo), the model and its licence, update check, and Open log folder.

## M7b — Beta to 1.0

### M7b-1 0.9 beta

- AC-1 Built by the M7a pipeline. Testers recruited by the maintainer; feedback through GitHub
  Issues plus an email address in the beta notes (plan §6).

### M7b-2 Triage the beta

- AC-1 Every beta finding is fixed or deferred with a reason, listed in plan §6.

### M7b-3 Release matrix

- AC-1 docs/testing.md's release test matrix is complete on the 1.0 draft, with every cell
  ticked.

### M7b-4 Update path

- AC-1 0.9 → 1.0 through the updater on every platform.

### M7b-5 Docs

- AC-1 The README states English only for 1.0, what leaves the machine (final list), and the
  first-open guide link. A user guide covers the core loop.

### M7b-6 Publish

- AC-1 The maintainer publishes the draft. `latest.json` goes live.
